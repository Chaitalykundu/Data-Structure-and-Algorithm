#include <iostream>
using namespace std;

int checkBit(int num, int pos)
{
    if (num &)
}

int main()
{
    int n, pos;
    cout << "Enter the number";
    cin >> n;

    cout << "\nEnter the position";
    cin >> pos;
    cout << "\n"
         << checkBit(n, pos);
    return 0;
}